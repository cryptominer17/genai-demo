# Raise inotify limits for Streamlit/watchdog file watchers.
# Each Streamlit app consumes hundreds of watches; the default (8192) is
# exhausted when multiple apps run on the same host.
fs.inotify.max_user_watches=524288
fs.inotify.max_user_instances=512
